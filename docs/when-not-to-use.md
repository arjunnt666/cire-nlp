# When not to use cire-nlp

if you need:
- multilingual generative models
- high quality embeddings for retrieval at scale
- instruction following chat

...use a proper model host.

cire-nlp is for narrow, inspectable pipelines where dependency weight and clarity matter more than SOTA scores.
